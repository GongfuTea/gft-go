package handlers

type RoleResolver struct {
}

// func (r *ResourceResolver) Login(cmd commands.UserLogin) (*auth.TokenDetails, error) {
// 	username := cmd.Username
// 	pass := cmd.Password
// 	return user.UserRepo.Login(username, pass)
// }
